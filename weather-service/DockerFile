FROM python:3.11-slim

# Evita prompts en apt (por si luego agregás paquetes del sistema)
ENV DEBIAN_FRONTEND=noninteractive
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# 1) Dependencias Python
COPY weather-service/requirements.txt /app/requirements.txt
RUN pip install --upgrade pip && \
    pip install --no-cache-dir -r /app/requirements.txt

# 2) Copiamos proto files (pre-compiled)
COPY proto/ /app/proto/

# 3) Copiamos el servidor
COPY weather-service/server.py /app/server.py

# 4) Runtime
ENV PYTHONPATH=/app
ENV WEATHER_PORT=50052
EXPOSE 50052

CMD ["python", "server.py"]
